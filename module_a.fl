mod = {
    drop = drop_;

    mod inner = super..module_c;

    test_value = 3;
};

drop_ n l = case n :: l of
    | 0 :: xs 		 -> xs
    | n :: (x :: xs) -> drop_ (n - 1) xs
    | otherwise		 -> ();


a = 2;
mod has_a = {
    a = super..a;
};
