mod = {
    drop = drop_;

    mod inner = has_a;
};

drop_ n l = case n :: l of
    | 0 :: xs 		 -> xs
    | n :: (x :: xs) -> drop_ (n - 1) xs
    | otherwise		 -> ();


mod has_a = {
    a = 2;
};
